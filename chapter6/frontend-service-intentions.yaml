apiVersion: consul.hashicorp.com/v1alpha1 
kind: ServiceIntentions
metadata:
  name: frontend
  namespace: consul 
spec:
  destination:
    name: "frontend"
  sources:
    - name: "ingress-gateway"
      action: allow
